var GeoJsfPlayer = {
		
		timestep: null,
		currentTime: null,

		play : function()
		{
			//Here needs to happen the following:
			//
			GeoJSF.map.setCenter(new OpenLayers.LonLat(lon, lat), zoom);
		},
		
		stop : function()
		{
			//Here needs to happen the following:
			//
			GeoJSF.map.setCenter(new OpenLayers.LonLat(lon, lat), zoom);
		},
		
		pause : function()
		{
			//Here needs to happen the following:
			//
			GeoJSF.map.setCenter(new OpenLayers.LonLat(lon, lat), zoom);
		},
		
		reset : function()
		{
			//Here needs to happen the following:
			//
			GeoJSF.map.setCenter(new OpenLayers.LonLat(lon, lat), zoom);
		},
};